{
    "name": "mtgo-scraper-monorepo",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build && cd .. && rm -rf dist && mv client/dist dist"
    },
    "devDependencies": {
        "vercel": "^32.0.0"
    },
    "dependencies": {
        "@google/generative-ai": "^0.24.1",
        "@libsql/client": "^0.15.15",
        "axios": "^1.13.2",
        "bcrypt": "^6.0.0",
        "better-sqlite3": "^12.5.0",
        "cheerio": "^1.1.2",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "helmet": "^8.1.0",
        "jsonwebtoken": "^9.0.3",
        "node-cron": "^4.2.1",
        "puppeteer": "^24.34.0",
        "puppeteer-extra": "^3.3.6",
        "puppeteer-extra-plugin-stealth": "^2.11.2"
    },
}